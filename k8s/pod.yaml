apiVersion: v1
kind: Pod
metadta:
  name: my-pod
spec:
  activeDeadlineSeconds: 2
  containners:
    - name: my-nginx
      image: nginx:1.25
      ports:
        - containerport: 80
          name: mani
          protocol: TCP